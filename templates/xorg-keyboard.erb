Section "InputClass"
  Identifier "system-keyboard"
  MatchIsKeyboard "on"
  Option "XkbLayout" "<%= @keyboard %>"
  Option "XkbOptions" "grp:win_space_toggle<% if @remap_caps %>, caps:escape<% end %>"
EndSection
